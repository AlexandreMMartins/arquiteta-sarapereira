# Leave empty to use the working email preparation flow.
# Enable only after testing a live contact endpoint with configured credentials.
PUBLIC_CONTACT_ENDPOINT=

# Server-only values for the optional Netlify contact function.
# Use a sender address verified with the email provider.
RESEND_API_KEY=
CONTACT_FROM=
CONTACT_ALLOWED_ORIGIN=
